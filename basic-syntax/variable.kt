fun main(args : Array<String>) {
    //Declaring a variable without type 
    val language = "English"  // Variable with key word val cannot be changed once the value is assigned
    var score = 95          // Variable with key word var can be changed later in the program

    //Declaring it with type
    val language2: Int 
    val score2: Double
}